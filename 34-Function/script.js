function sayHelloWorld() {
  // use "function" keyword then "nameFunction()" then { "block of code that we want to do" }
  document.writeln("<p>Hello World</p>");
}

// then just call the name of the function to run the code that we already wrote for that function
sayHelloWorld();
sayHelloWorld();
sayHelloWorld(); // do not forget the ()
// you can call it as many times as you want (depends on your needs)

// note: if we do not call it, it will never execute. so call it
