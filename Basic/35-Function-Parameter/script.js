function sayHello(firstName, lastName) {
  // firstName & lastName are the parameters
  document.writeln(`<p>Hello ${firstName} ${lastName}</p>`);
}

sayHello("Rama", "Putra"); // when we call the function, do not forget to give the value for those parameter firstName & lastName
sayHello("Assela", "Sutrisno");
