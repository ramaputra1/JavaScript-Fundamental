{
  let name = null;
  if (name === null) {
    document.writeln("It is Null");
  } else if (name === undefined) {
    document.writeln("It is Undefined"); // It's showed up, so variable "name" is NOT undefined cause it's initialized
  }
}
